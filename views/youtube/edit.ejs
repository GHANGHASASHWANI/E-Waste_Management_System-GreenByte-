
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Video</title>
  
  <!-- Tailwind CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      padding-top: 4rem; /* Adjust this value based on your navbar's height */
    }
    .main-content {
      margin-bottom: 4rem; /* Space between content and footer */
      margin-top: 5rem;
    }
  </style>
</head>

<body class="bg-gray-100">

  <!-- Include Navbar -->
  <%- include('../includes/navbar') %>

  <!-- Main Content -->
  <div class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg main-content">
    <h2 class="text-2xl font-bold text-center text-gray-700 mb-8">Update Video</h2>
    <form action="/youtube/update/<%= video._id %>" method="POST" novalidate class="space-y-6 needs-validation">

      <div class="form-group">
        <label for="title" class="block text-gray-700 font-semibold mb-2">Title:</label>
        <input type="text" name="title" id="title" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= video.title %>" required>
      </div>

      <div class="form-group">
        <label for="videoUrl" class="block text-gray-700 font-semibold mb-2">Video URL:</label>
        <input type="url" name="videoUrl" id="videoUrl" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= video.videoUrl %>" required>
      </div>

      <div class="form-group">
        <label for="description" class="block text-gray-700 font-semibold mb-2">Description:</label>
        <textarea name="description" id="description" class="form-input w-full p-2 border border-gray-300 rounded" rows="4"><%= video.description %></textarea>
      </div>

      <div class="form-group">
        <label for="imageUrl" class="block text-gray-700 font-semibold mb-2">Image URL:</label>
        <input type="url" name="imageUrl" id="imageUrl" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= video.imageUrl %>">
      </div>

      <div class="form-group">
        <label for="views" class="block text-gray-700 font-semibold mb-2">Views:</label>
        <input type="number" name="views" id="views" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= video.views %>">
      </div>

      <div class="form-group">
        <label for="likes" class="block text-gray-700 font-semibold mb-2">Likes:</label>
        <input type="number" name="likes" id="likes" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= video.likes %>">
      </div>

      <div class="form-group">
        <label for="dislikes" class="block text-gray-700 font-semibold mb-2">Dislikes:</label>
        <input type="number" name="dislikes" id="dislikes" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= video.dislikes %>">
      </div>

      <div class="btn-container text-center">
        <button type="submit" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">Update Video</button>
      </div>
    </form>
  </div>

  <!-- Include Footer -->
  <%- include('../includes/footer') %>

</body>

</html>
