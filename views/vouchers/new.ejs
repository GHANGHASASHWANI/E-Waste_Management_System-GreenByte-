<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Voucher</title>
    
    <!-- Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

    <style>
        body {
            padding-top: 4rem; /* Adjust this value based on your navbar's height */
        }
        .main-content {
            margin-bottom: 4rem; /* Space between content and footer */
        }
    </style>
</head>
<body class="bg-green-50">

    <!-- Include Navbar -->
    <%- include('../includes/navbar') %>

    <!-- Main Content Container -->
    <div class="main-content max-w-4xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
        <h1 class="text-2xl font-bold text-center text-green-600 mb-8">Create New Voucher</h1>
        <p class="text-center text-green-900 mb-6">Please fill out the form below to create a new voucher.</p>

        <form action="/vouchers" method="POST" class="space-y-6">
            
            <!-- Voucher Code -->
            <div class="form-group">
                <label for="code" class="block text-gray-700 font-semibold mb-2">Code:</label>
                <input type="text" name="voucher[code]" id="code" class="form-input w-full p-2 border border-gray-300 rounded" placeholder="Enter voucher code" required>
            </div>

            <!-- Discount -->
            <div class="form-group">
                <label for="discount" class="block text-gray-700 font-semibold mb-2">Discount (%):</label>
                <input type="number" name="voucher[discount]" id="discount" class="form-input w-full p-2 border border-gray-300 rounded" placeholder="Enter discount percentage" required>
            </div>

            <!-- Ecoscore Required -->
            <div class="form-group">
                <label for="ecoscoreRequired" class="block text-gray-700 font-semibold mb-2">Ecoscore Required:</label>
                <input type="number" name="voucher[ecoscoreRequired]" id="ecoscoreRequired" class="form-input w-full p-2 border border-gray-300 rounded" placeholder="Enter ecoscore required" required>
            </div>

            <!-- Description -->
            <div class="form-group">
                <label for="description" class="block text-gray-700 font-semibold mb-2">Description:</label>
                <textarea name="voucher[description]" id="description" class="form-input w-full p-2 border border-gray-300 rounded" rows="4" placeholder="Enter voucher description"></textarea>
            </div>

            <!-- Expiry Date -->
            <div class="form-group">
                <label for="expiryDate" class="block text-gray-700 font-semibold mb-2">Expiry Date:</label>
                <input type="date" name="voucher[expiryDate]" id="expiryDate" class="form-input w-full p-2 border border-gray-300 rounded">
            </div>

            <!-- URL -->
            <div class="form-group">
                <label for="url" class="block text-gray-700 font-semibold mb-2">URL:</label>
                <input type="url" name="voucher[url]" id="url" class="form-input w-full p-2 border border-gray-300 rounded" placeholder="Enter voucher URL">
            </div>

            <!-- Submit Button -->
            <div class="text-center">
                <button type="submit" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">Create Voucher</button>
            </div>
        </form>
    </div>

    <!-- Include Footer -->
    <%- include('../includes/footer') %>

</body>
</html>
