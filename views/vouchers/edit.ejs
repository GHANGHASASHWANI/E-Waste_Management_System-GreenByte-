<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Voucher</title>
</head>
<body>
    <h1>Edit Voucher</h1>
    <form action="/vouchers/<%= voucher._id %>?_method=PUT" method="POST">
        <label for="code">Code:</label>
        <input type="text" id="code" name="voucher[code]" value="<%= voucher.code %>" required><br>

        <label for="discount">Discount (%):</label>
        <input type="number" id="discount" name="voucher[discount]" value="<%= voucher.discount %>" required><br>

        <label for="ecoscoreRequired">Ecoscore Required:</label>
        <input type="number" id="ecoscoreRequired" name="voucher[ecoscoreRequired]" value="<%= voucher.ecoscoreRequired %>" required><br>

        <label for="description">Description:</label>
        <textarea id="description" name="voucher[description]"><%= voucher.description %></textarea><br>

        <label for="expiryDate">Expiry Date:</label>
        <input type="date" id="expiryDate" name="voucher[expiryDate]" value="<%= voucher.expiryDate ? voucher.expiryDate.toISOString().split('T')[0] : '' %>"><br>

        <button type="submit">Update Voucher</button>
    </form>
    <a href="/vouchers">Back to Vouchers</a>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Voucher</title>
    
    <!-- Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 4rem; /* Adjust this value based on your navbar's height */
        }
        .main-content {
            margin-bottom: 4rem; /* Space between content and footer */
            margin-top: 5rem;
        }
    </style>
</head>
<body class="bg-green-50">
    <!-- Include Navbar -->
    <%- include('../includes/navbar') %>

    <!-- Main Content -->
    <div class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg main-content">
        <h2 class="text-2xl font-bold text-center text-green-600 mb-8">Edit Voucher</h2>
        <form action="/vouchers/<%= voucher._id %>?_method=PUT" method="POST" class="space-y-6">
            
            <div class="form-group">
                <label for="code" class="block text-gray-700 font-semibold mb-2">Code:</label>
                <input type="text" name="voucher[code]" id="code" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= voucher.code %>" required>
            </div>

            <div class="form-group">
                <label for="discount" class="block text-gray-700 font-semibold mb-2">Discount (%):</label>
                <input type="number" name="voucher[discount]" id="discount" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= voucher.discount %>" required>
            </div>

            <div class="form-group">
                <label for="ecoscoreRequired" class="block text-gray-700 font-semibold mb-2">Ecoscore Required:</label>
                <input type="number" name="voucher[ecoscoreRequired]" id="ecoscoreRequired" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= voucher.ecoscoreRequired %>" required>
            </div>

            <div class="form-group">
                <label for="description" class="block text-gray-700 font-semibold mb-2">Description:</label>
                <textarea name="voucher[description]" id="description" class="form-input w-full p-2 border border-gray-300 rounded" rows="4"><%= voucher.description %></textarea>
            </div>

            <div class="form-group">
                <label for="expiryDate" class="block text-gray-700 font-semibold mb-2">Expiry Date:</label>
                <input type="date" name="voucher[expiryDate]" id="expiryDate" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= voucher.expiryDate ? voucher.expiryDate.toISOString().split('T')[0] : '' %>">
            </div>

            <div class="form-group">
                <label for="url" class="block text-gray-700 font-semibold mb-2">Image URL:</label>
                <input type="text" name="voucher[url]" id="url" class="form-input w-full p-2 border border-gray-300 rounded" value="<%= voucher.url %>">
            </div>

            <div class="text-center">
                <button type="submit" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">Update Voucher</button>
            </div>
        </form>
        <div class="text-center mt-4">
            <a href="/vouchers" class="text-green-500 hover:underline">Back to Vouchers</a>
        </div>
    </div>

    <!-- Include Footer -->
    <%- include('../includes/footer') %>
</body>
</html>

